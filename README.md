# cpp_modules
42 Vienna assigments.

Collection of all the C++ piscine like projects 

### Useful commands

nice and quick way to clean all the builds:

```
for i in `find -maxdepth 2 -type d | grep /ex`; do make -C $i fclean; done
```
